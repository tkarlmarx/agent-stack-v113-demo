# demo app

Created by the agent-stack git walkthrough on 2026-09-03 to exercise clone,
branch, commit, push and pull-request through the GitHub App's own installation
token — the same credential path a workspace agent uses.

    python3 app/main.py
    cd app && python3 -m pytest -q
